#include <stdio.h>

int main(){


    int n = malloc(n * sizeof(int));

    scanf("%d", &n);

    int array[n];

    for(int i = 0; i < n; i++){

        scanf("%d", &array[i]);

    }



    return 0;
}